---
# Replace BOARD_NAME_HERE with the exact ZMK board name for the controllers
# fitted to the keyboard (for example, the name selected by `zmk keyboard add`).
include:
  - board: nice_nano_v2
    shield: sweep_left
  - board: nice_nano_v2
    shield: sweep_right
